<nav class="navbar-home navbar navbar-default" role="navigation">
    <div class="container">
        <div class="header-info row">
            <div class="header-item">
                <i class="icon glyphicon glyphicon-envelope"></i>
                <span class="header-item-text">development@orderswyft.com</span>
            </div>
            <div class="navbar-float">
                <a href="https://www.facebook.com/orderswyft"><img src="images/facebook.png" height="20" width="20"></a>
                <img src="images/twitter.png" height="20" width="20">
            </div>
        </div>
        <div class="navbar-main row">
            <div class="navbar-header">
                <a class="navbar-brand-home navbar-brand" href="/">SWYFT<span class="logo-light">DELIVERY</span></a>
            </div>
        </div>
    </div>
</nav>

<div class="img-background container-fluid">
    <h1 class="background-title">Delivery.</h1>
</div>

<div class="container">
    <ul class="breadcrumb">
        <li><a>{{#link-to 'index'}}Home{{/link-to}}</a></li>
        <li><a>{{#link-to 'delivery'}}Delivery{{/link-to}}</a></li>
        <li class="active">Order</li>
    </ul>
    <h4>Need Assistance? Call Bhargav at <a href="tel:603-793-7333">603-793-7333</a></h4>
    <div class="info-well well">
        <h1>{{order.user.firstName}} {{order.user.lastName}}</h1>
        <h3>{{order.deliveryLocation}}</h3>
        <h3><a {{bind-attr href=order.phoneLink}}>{{order.contactPhone}}</a></h3>
        <h3>{{order.userComments}}</h3>
        <h3>{{render-decimals order.actualAmount}}</h3>
        <h3>{{order.paymentType}}</h3>
        <ul class="item-list">
            {{#each item in order.items}}
            <li>
                <div>
                    <p><strong>{{item.restaurant}}</strong></p>
                    <p>Name: {{item.name}}</p>
                    <p>Item Options: {{item.itemOptions}}</p>
                    <p>Requests: {{item.additionalRequests}}</p>
                    <p>{{render-decimals item.price}}</p>
                </div>
            </li>
            {{/each}}
        </ul>
    </div>
</div>
