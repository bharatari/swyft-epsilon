<nav class="navbar-home navbar navbar-default" role="navigation">
    <div class="container">
        <div class="header-info row">
            <div class="header-item">
                <i class="icon glyphicon glyphicon-envelope"></i>
                <span class="header-item-text">development@orderswyft.com</span>
            </div>
            <div class="navbar-float">
                <a href="https://www.facebook.com/orderswyft"><img src="images/facebook.png" height="20" width="20"></a>
                <img src="images/twitter.png" height="20" width="20">
            </div>
        </div>
        <div class="navbar-main row">
            <div class="navbar-header">
                <a class="navbar-brand-home navbar-brand" href="/">SWYFT<span class="logo-light">ADMIN</span></a>
            </div>
        </div>
    </div>
</nav>

<div class="container">
    <table class="table table-hover">
        <tr>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Comments</th>
            <th>Total</th>
            <th>Location</th>
            <th>Phone</th>
            <th>Payment</th>
            <th>Items</th>
        </tr>
        {{#each order in deliveryItems}}
        <tr>
            <td>{{order.user.firstName}}</td>
            <td>{{order.user.lastName}}</td>
            <td>{{order.userComments}}</td>
            <td>{{render-decimals order.actualAmount}}</td>
            <td>{{order.deliveryLocation}}</td>
            <td>{{order.contactPhone}}</td>
            <td>{{order.paymentType}}</td>
            <td>
                <ul>
                    {{#each item in order.items}}
                    <li>
                        <div>
                            <p><strong>{{item.restaurant}}</strong></p>
                            <p>Name: {{item.name}}</p>
                            <p>Item Options: {{item.itemOptions}}</p>
                            <p>Requests: {{item.additionalRequests}}</p>
                            <p>{{render-decimals item.price}}</p>
                        </div>
                    </li>
                    {{/each}}
                </ul>
            </td>
        </tr>
        {{/each}}
    </table>
    
</div>
